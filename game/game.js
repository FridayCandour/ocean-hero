// const sea_world = re.build("sea_world"),
// sea = buildCanvas("sea_hero_canvas"); 
// sea_world.append(sea) 
// let hero = new entity("hero", new spriteSheetPainter("hero.png",4,3,5));
// u(hero).config({top: 90,left:  50,width: 190,height: 80})
// renderer.assemble(hero)
// renderer.render(sea); 
// re.mount(sea_world);
// re.start();